import React from 'react';

const InventoryPage = (props) => {

    return(<div>Hello from inventory!!!</div>);
    
        /* let redirectToLogin = <Redirect to="login"/>;

        return(redirectToLogin); */
}

export default InventoryPage;